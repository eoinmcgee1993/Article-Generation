# MCP Server

Phase 1 contract for the Substack Content OS. Implement with the MCP TypeScript SDK and Streamable HTTP transport. Keep publishing behind authenticated approval gates. The tool names in src/server.ts are the stable application contract.